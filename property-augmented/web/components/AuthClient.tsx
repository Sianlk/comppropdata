'use client';import{useState}from'react';import{api}from'@/lib/api';import{useRouter}from'next/navigation';export default function AuthClient(){const[mode,setMode]=useState<'login'|'register'>('login'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[name,setName]=useState(''),[error,setError]=useState('');const r=useRouter();async function submit(){setError('');try{const x:any=await api(`/api/v1/auth/${mode}`,{method:'POST',body:JSON.stringify({email,password,name})});localStorage.setItem('pda_token',x.token);r.push('/app/projects')}catch(e:any){setError(e.message)}}return <div className="toolWorkbench"><p><button onClick={()=>setMode('login')}>Sign in</button> · <button onClick={()=>setMode('register')}>Create account</button></p>{mode==='register'&&<label>Name<input value={name} onChange={e=>setName(e.target.value)}/></label>}<label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="button" onClick={submit}>{mode==='login'?'Sign in':'Create account'}</button>{error&&<div className="error">{error}</div>}</div>}

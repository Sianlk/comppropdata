export const API_BASE=process.env.NEXT_PUBLIC_API_BASE_URL||'';
type Options=RequestInit&{timeoutMs?:number};
export async function api<T=any>(path:string,options:Options={}):Promise<T>{if(!API_BASE)throw new Error('Live API is not configured. Set NEXT_PUBLIC_API_BASE_URL.');const controller=new AbortController();const timeout=setTimeout(()=>controller.abort(),options.timeoutMs??45000);const token=typeof window!=='undefined'?localStorage.getItem('pda_token'):null;try{const res=await fetch(`${API_BASE}${path}`,{...options,signal:controller.signal,headers:{...(options.body instanceof FormData?{}:{'Content-Type':'application/json'}),...(token?{Authorization:`Bearer ${token}`}:{}) ,...(options.headers||{})}});if(!res.ok){const text=await res.text();throw new Error(text||`Request failed ${res.status}`)}const ct=res.headers.get('content-type')||'';if(ct.includes('application/json'))return res.json();return(await res.blob())as T}finally{clearTimeout(timeout)}}

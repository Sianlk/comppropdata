import type{Metadata}from'next';import Link from'next/link';import{notFound}from'next/navigation';import{insightMap,insights}from'@/lib/insights';import{toolMap}from'@/lib/tools';
const toolAliases:Record<string,string>={'residual-land-value-calculator':'residual-land-value'};
export function generateStaticParams(){return insights.map(x=>({slug:x.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const a=insightMap[slug];if(!a)return{};return{title:a.title,description:a.description,alternates:{canonical:`/insights/${slug}`},openGraph:{type:'article',title:a.title,description:a.description,publishedTime:a.published,modifiedTime:a.reviewed,siteName:'Property Development, Augmented'}}}
export default async function InsightPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const a=insightMap[slug];if(!a)notFound();const resolvedTool=toolAliases[a.tool]||a.tool;const tool=toolMap[resolvedTool];const workflowHref=tool?`/app/${tool.slug}`:'/app/development-intelligence';const workflowTitle=tool?.title||'Development Intelligence HQ';const article={'@context':'https://schema.org','@type':'Article',headline:a.title,description:a.description,datePublished:a.published,dateModified:a.reviewed,author:{'@type':'Organization',name:'Property Development, Augmented Research Team',url:'https://propertydevelopmentaugmented.co.uk/about'},publisher:{'@type':'Organization',name:'Property Development, Augmented',alternateName:'UK Planning & Development Intelligence',url:'https://propertydevelopmentaugmented.co.uk'}};return <main><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(article)}}/><article className="article"><div className="eyebrow">PROPERTY DEVELOPMENT, AUGMENTED · UK PLANNING & DEVELOPMENT INTELLIGENCE</div><h1>{a.title}</h1><p className="lede">{a.description}</p><p className="muted">Published {a.published} · Reviewed {a.reviewed} · Property Development, Augmented Research Team</p>{a.sections.map(s=><section key={s.heading}><h2>{s.heading}</h2>{s.body.map(p=><p key={p}>{p}</p>)}</section>)}{a.sources.length>0&&<section className="panel sources"><h2>Primary / authoritative sources</h2>{a.sources.map(s=><p key={s.url}><a href={s.url} target="_blank" rel="noreferrer">{s.name} ↗</a></p>)}<p className="muted">Source links are provided for verification. Always check the current authoritative document before relying on policy or time-sensitive data.</p></section>}<section><h2>Use the workflow.</h2><p>Turn the principle into a repeatable UK property-development workflow in the working platform.</p><Link className="button" href={workflowHref}>Open {workflowTitle}</Link></section></article></main>}

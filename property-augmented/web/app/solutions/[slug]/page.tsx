import type{Metadata}from'next';import Link from'next/link';import{notFound}from'next/navigation';import{solutionMap,solutions}from'@/lib/solutions';export function generateStaticParams(){return solutions.map(x=>({slug:x.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const s=solutionMap[slug];if(!s)return{};return{title:s.seoTitle,description:s.description,alternates:{canonical:`/solutions/${slug}`}}}export default async function SolutionPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const s=solutionMap[slug];if(!s)notFound();const schema={'@context':'https://schema.org','@type':'Service',name:s.seoTitle,provider:{'@type':'Organization',name:'Property Development, Augmented'},audience:{'@type':'Audience',audienceType:s.audience},description:s.description};return <main><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><section className="hero compact"><div className="heroKicker">{s.audience.toUpperCase()}</div><h1>{s.title}</h1><p className="lede">{s.description}</p><div className="actions"><Link className="button premium" href={s.tools[0].href}>{s.tools[0].label}</Link><Link className="button ghostOnDark" href="/solutions">All solutions</Link></div></section><section className="premiumBand"><div><div className="eyebrow warm">THE PROBLEM</div><h2>{s.problem}</h2></div><p>The answer is not another isolated AI prompt. It is a workflow that keeps source, date, assumption, calculation and decision state visible from first screen to final action.</p></section><section><div className="sectionHead"><div><div className="eyebrow">WHAT THE SYSTEM PRODUCES</div><h2>Useful work product.</h2></div></div><div className="grid">{s.outcomes.map((x,i)=><article className="card" key={x}><small>{String(i+1).padStart(2,'0')}</small><h3>{x}</h3></article>)}</div></section><section className="panel"><div className="eyebrow">WORKFLOW</div><h2>A repeatable route from question to evidence.</h2><div className="journey">{s.workflow.map((x,i)=><article key={x}><strong>{String(i+1).padStart(2,'0')}</strong><h3>{x}</h3></article>)}</div></section><section><div className="eyebrow">BUILT-IN TOOLS</div><h2>Use the operating system, not a separate microsite.</h2><div className="grid">{s.tools.map(t=><Link className="card" href={t.href} key={t.href}><h3>{t.label}</h3><span>Open →</span></Link>)}</div></section><section className="dark"><div><div className="eyebrow warm">NEXT DECISION</div><h2>Start with the evidence you already have.</h2><p>For a live development opportunity, a postcode is enough to begin the evidence sequence. Add the full address, proposal and commercial assumptions as they become established.</p></div><Link className="button light" href="/app/development-intelligence">Run Development Intelligence HQ</Link></section></main>}

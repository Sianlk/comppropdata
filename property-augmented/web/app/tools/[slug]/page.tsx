import type {Metadata} from 'next';import {notFound} from 'next/navigation';import Link from 'next/link';import {toolMap,tools} from '@/lib/tools';
export function generateStaticParams(){return tools.map(t=>({slug:t.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const t=toolMap[slug];if(!t)return{};return{title:t.publicTitle,description:t.meta,keywords:t.keywords,alternates:{canonical:`/tools/${slug}`},openGraph:{title:t.publicTitle,description:t.meta,url:`/tools/${slug}`}}}export default async function ToolLanding({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const t=toolMap[slug];if(!t)notFound();const schema={'@context':'https://schema.org','@type':'SoftwareApplication',name:t.publicTitle,applicationCategory:'BusinessApplication',operatingSystem:'Web',description:t.meta};return <main><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><section className="hero compact"><span className={`stage ${t.stage.toLowerCase()}`}>{t.stage}</span><h1>{t.publicTitle}</h1><p className="lede">{t.short}</p><div className="actions"><Link className="button" href={`/app/${t.slug}`}>Open {t.title}</Link><Link className="button ghost" href="/pricing">See consultancy</Link></div></section><section><h2>Built for evidence-led decisions.</h2><div className="featureGrid"><article><h3>Source traceability</h3><p>Live-data results show provider, source URL or identifier, retrieval time and caveat.</p></article><article><h3>Human verification</h3><p>Material facts, numbers, policy and professional conclusions pass through a human review gate.</p></article><article><h3>Independent architecture</h3><p>The production request path is browser → Next.js → FastAPI; Lovable is optional.</p></article></div></section><section className="panel"><h2>Search topics addressed</h2><p>{t.keywords.join(' · ')}</p></section></main>}

import type{MetadataRoute}from'next';import{tools}from'@/lib/tools';import{seoPages}from'@/lib/seoPages';export default function sitemap():MetadataRoute.Sitemap{const base=(process.env.NEXT_PUBLIC_SITE_URL||'https://propertydevelopmentaugmented.co.uk').replace(/\/$/,'');const now=new Date();const routes=['','/pricing','/resources','/resources/site-triage','/products/ai-property-developer-os','/contact','/book','/media',...tools.map(t=>`/tools/${t.slug}`),...seoPages.map(p=>`/tools/${p.slug}`)];return Array.from(new Set(routes)).map(url=>({url:base+url,lastModified:now,changeFrequency:url.includes('/tools/')?'weekly':'monthly',priority:url===''?1:(url.includes('/tools/')?0.85:0.7)}))}

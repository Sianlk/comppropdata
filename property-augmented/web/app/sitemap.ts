import type {MetadataRoute} from 'next';import {tools} from '@/lib/tools';export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||'https://propertydevelopmentaugmented.co.uk';const now=new Date();return['','/pricing','/resources',...tools.map(t=>`/tools/${t.slug}`)].map(url=>({url:base+url,lastModified:now,changeFrequency:url.includes('/tools/')?'weekly':'monthly',priority:url===''?1:(url.includes('/tools/')?0.8:0.7)}))}

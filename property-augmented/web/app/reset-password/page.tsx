import{ResetPasswordClient}from'@/components/PasswordResetClients';export const metadata={title:'Choose a new password',robots:{index:false,follow:false}};export default async function Reset({searchParams}:{searchParams:Promise<{token?:string|string[]}>}){const q=await searchParams;const token=Array.isArray(q.token)?q.token[0]||'':q.token||'';return <main><section className="hero compact"><div className="eyebrow">ACCOUNT</div><h1>Choose a new password.</h1><p className="lede">Reset links expire after 45 minutes and can be used once.</p><ResetPasswordClient token={token}/></section></main>}
